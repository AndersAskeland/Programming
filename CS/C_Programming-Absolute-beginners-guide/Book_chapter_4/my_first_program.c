#include <stdio.h>

// printf contains the following (printf (controlString [, data])) - The bracket part is optional and you dont need the brackets
int main(void) {
  printf("hello,  world\n"); // the ; allows C to know that this line is over - when somethis executes.
  printf("Hej Louise dasdas \n");
  printf("My favorite number is %d\n", 8); // Prints the number [data]. Maybe the %d stands for data.
}

/* \n is an escape statement that creates a new line. There are many more escape statements. */

